rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Both apps now require Firebase Auth login before any Firestore
      // access happens, so require an authenticated request here too.
      allow read, write: if request.auth != null;
    }
  }
}
